# INVITE NetSuite API Integration

## Overview
Ths project leverages SQLModel/SQLAlchemy to interact with the NetSuite API. The goal is to provide a simple interface to interact with the NetSuite API using SuiteQL.

### Available Tables/Fields
These can be found in the NetSuite UI under Setup -> Records Catalog
